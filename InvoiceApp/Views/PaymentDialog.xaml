<Window x:Class="InvoiceApp.Views.PaymentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Record Payment" 
        Width="500"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        MinHeight="350"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Record Payment" 
                      FontSize="20" 
                      FontWeight="Bold"
                      Foreground="#2C3E50"/>
            <TextBlock x:Name="InvoiceInfoText" 
                      Text="Invoice: INV-0000" 
                      FontSize="14" 
                      Foreground="#7F8C8D"
                      Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Payment Details -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Amount Due:" FontWeight="Bold"/>
            <TextBlock Grid.Row="0" Grid.Column="1" 
                      x:Name="AmountDueText" 
                      Text="$0.00" 
                      FontSize="20"
                      FontWeight="Bold"
                      Foreground="#E74C3C"
                      VerticalAlignment="Center"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Payment Amount: *" FontWeight="Bold" Margin="0,15,0,0"/>
            <TextBox Grid.Row="1" Grid.Column="1" 
                    x:Name="PaymentAmountTextBox" 
                    Height="35"
                    FontSize="14"
                    Margin="0,15,0,0"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Payment Date: *" FontWeight="Bold" Margin="0,10,0,0"/>
            <DatePicker Grid.Row="2" Grid.Column="1" 
                       x:Name="PaymentDatePicker" 
                       Height="35"
                       Margin="0,10,0,0"/>

            <Label Grid.Row="3" Grid.Column="0" Content="Notes:" FontWeight="Bold" Margin="0,10,0,0"/>
            <TextBox Grid.Row="3" Grid.Column="1" 
                    x:Name="NotesTextBox" 
                    Height="60"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    VerticalScrollBarVisibility="Auto"
                    Margin="0,10,0,0"/>
        </Grid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                   Orientation="Horizontal" 
                   HorizontalAlignment="Right" 
                   Margin="0,20,0,0">
            <Button Content="💰 Record Payment" 
                   Width="150" 
                   Height="40"
                   Background="#2ECC71"
                   Foreground="White"
                   BorderThickness="0"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Click="RecordPaymentButton_Click" 
                   IsDefault="True"
                   Margin="0,0,10,0"/>
            <Button Content="Cancel" 
                   Width="100" 
                   Height="40"
                   Background="#95A5A6"
                   Foreground="White"
                   BorderThickness="0"
                   FontSize="14"
                   Click="CancelButton_Click" 
                   IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
<UserControl x:Class="InvoiceApp.Views.StatementListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Statements" Style="{StaticResource TitleTextBlock}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Generate Statement" Command="{Binding GenerateStatementCommand}"/>
            <Button Content="View" Command="{Binding ViewStatementCommand}"/>
            <Button Content="Delete" Command="{Binding DeleteStatementCommand}"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Statements}" SelectedItem="{Binding SelectedStatement}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Client" Binding="{Binding Client.CompanyName}" Width="*"/>
                <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate, StringFormat=d}" Width="100"/>
                <DataGridTextColumn Header="End Date" Binding="{Binding EndDate, StringFormat=d}" Width="100"/>
                <DataGridTextColumn Header="Opening Balance" Binding="{Binding OpeningBalance, StringFormat=C}" Width="120"/>
                <DataGridTextColumn Header="Closing Balance" Binding="{Binding ClosingBalance, StringFormat=C}" Width="120"/>
                <DataGridTextColumn Header="Created" Binding="{Binding CreatedAt, StringFormat=d}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
